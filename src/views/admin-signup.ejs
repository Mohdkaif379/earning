<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Signup · refined split</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* subtle animated white background (as requested previously) */
    @keyframes softBreath {
      0% { background-color: #ffffff; }
      50% { background-color: #f8f4ec; } /* warm hint */
      100% { background-color: #ffffff; }
    }
    .animate-white-pulse {
      animation: softBreath 8s ease-in-out infinite;
    }
    /* very light dot texture for depth */
    .bg-dots-soft {
      background-image: radial-gradient(rgba(140, 110, 70, 0.05) 1px, transparent 1px);
      background-size: 34px 34px;
    }
    .card-elevation {
      box-shadow: 0 20px 30px -12px rgba(70, 50, 30, 0.2);
    }
  </style>
</head>
<body class="min-h-screen flex items-center justify-center p-4 sm:p-6 animate-white-pulse bg-dots-soft">

  <!-- main split container: same style as login, warm/white harmony -->
  <div class="w-full max-w-5xl mx-auto flex flex-col md:flex-row rounded-3xl overflow-hidden bg-white/90 backdrop-blur-sm card-elevation border border-amber-200/60">

    <!-- LEFT SIDE: content area (warm and welcoming) -->
    <div class="w-full md:w-1/2 bg-[#fdf0d5] p-7 sm:p-9 flex flex-col justify-between relative">
      <!-- subtle decorative pattern (barely there) -->
      <div class="absolute inset-0 opacity-10 pointer-events-none" style="background: repeating-linear-gradient(45deg, #a57c5a 0px, #a57c5a 2px, transparent 2px, transparent 16px);"></div>

      <div class="relative z-10">
        <!-- badge / tagline -->
        <span class="inline-block bg-amber-200/70 text-amber-900 text-xs font-semibold px-3 py-1.5 rounded-full mb-5 border border-amber-300/50 backdrop-blur-sm">✨ new administrator</span>

        <h2 class="text-3xl sm:text-4xl font-bold text-amber-950 leading-tight">Create account,<br>get started</h2>

        <p class="mt-4 text-amber-800/90 text-base sm:text-lg max-w-sm leading-relaxed">
          Join the admin team. Manage products, orders, and insights – all in one place.
        </p>

        <!-- short feature list (trust builders) -->
        <ul class="mt-8 space-y-3 text-amber-900">
          <li class="flex items-center gap-3">
            <span class="w-5 h-5 rounded-full bg-amber-600/20 flex items-center justify-center text-amber-800 text-sm font-bold">✓</span>
            <span class="text-sm sm:text-base">Full inventory control</span>
          </li>
          <li class="flex items-center gap-3">
            <span class="w-5 h-5 rounded-full bg-amber-600/20 flex items-center justify-center text-amber-800 text-sm font-bold">✓</span>
            <span class="text-sm sm:text-base">Order & customer overview</span>
          </li>
          <li class="flex items-center gap-3">
            <span class="w-5 h-5 rounded-full bg-amber-600/20 flex items-center justify-center text-amber-800 text-sm font-bold">✓</span>
            <span class="text-sm sm:text-base">Team permissions & logs</span>
          </li>
        </ul>
      </div>

      <!-- friendly footnote (matches login page style) -->
      <div class="relative z-10 mt-8 text-sm italic text-amber-700/70 border-l-3 border-amber-400 pl-4">
        “Secure, fast, and built for your team.”
      </div>
    </div>

    <!-- RIGHT SIDE: white panel with signup form (clean, same warm accents) -->
    <div class="w-full md:w-1/2 bg-white p-7 sm:p-9 flex items-center">
      <div class="w-full max-w-sm mx-auto">
        <h3 class="text-xl font-semibold text-amber-900 mb-1">Admin signup</h3>
        <p class="text-sm text-amber-700/80 mb-5">Fill in the details to register.</p>

        <!-- error placeholder (compatible with ejs, hidden by default) -->
        <% if (typeof adminError !== "undefined" && adminError) { %>
        <div class="mb-4 text-sm text-red-700 bg-red-100 border border-red-300 rounded-lg px-3 py-2"><%= adminError %></div>
        <% } else { %>
        <!-- hidden dummy for layout consistency – no error shown -->
        <div class="mb-4 text-sm text-red-700 bg-red-100 border border-red-300 rounded-lg px-3 py-2 hidden">error placeholder</div>
        <% } %>

        <!-- form with refined fields (same warm inputs as login) -->
        <form action="/admin/signup" method="POST" class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-amber-800 mb-1.5">Username</label>
            <input
              type="text"
              name="username"
              required
              autocomplete="username"
              class="w-full border border-amber-300 rounded-lg px-3.5 py-3 text-amber-950 bg-[#fdf0d5] placeholder-amber-700 outline-none focus:border-amber-500 focus:ring-2 focus:ring-amber-300/60"
              placeholder="admin123"
            />
          </div>

          <div>
            <label class="block text-sm font-medium text-amber-800 mb-1.5">Password</label>
            <input
              type="password"
              name="password"
              required
              autocomplete="new-password"
              class="w-full border border-amber-300 rounded-lg px-3.5 py-3 text-amber-950 bg-[#fdf0d5] placeholder-amber-700 outline-none focus:border-amber-500 focus:ring-2 focus:ring-amber-300/60"
              placeholder="••••••••"
            />
          </div>

          <div>
            <label class="block text-sm font-medium text-amber-800 mb-1.5">Confirm password</label>
            <input
              type="password"
              name="confirmPassword"
              required
              autocomplete="off"
              class="w-full border border-amber-300 rounded-lg px-3.5 py-3 text-amber-950 bg-[#fdf0d5] placeholder-amber-700 outline-none focus:border-amber-500 focus:ring-2 focus:ring-amber-300/60"
              placeholder="••••••••"
            />
          </div>

          <button type="submit" class="w-full bg-amber-700 hover:bg-amber-800 text-[#fdf0d5] py-3 rounded-lg font-semibold transition shadow-sm hover:shadow-md mt-2">
            Create account
          </button>
        </form>

        <!-- extra links: login & maybe terms (subtle) -->
        <div class="mt-6 flex flex-wrap items-center justify-between gap-2 text-sm">
          <a href="/admin/login" class="text-amber-700 hover:text-amber-800 underline font-medium">← Back to login</a>
          <a href="#" class="text-amber-600 hover:text-amber-700">Privacy & terms</a>
        </div>

        <p class="text-xs text-amber-500/70 mt-6 text-center">© 2025 · secure registration</p>
      </div>
    </div>
  </div>

  <!-- note: background animation remains on body (white pulse) as previously designed -->
</body>
</html>