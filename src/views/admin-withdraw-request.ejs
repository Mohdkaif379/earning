<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Withdrawal Request</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="min-h-screen bg-[#fdf0d5]">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-8">
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 mb-6">
      <div>
        <h1 class="text-2xl font-semibold text-amber-950">Withdrawal Request Detail</h1>
        <p class="text-amber-700 text-sm">Admin: <%= username %></p>
      </div>
      <a href="/admin-dashboard" class="w-full sm:w-auto text-center bg-amber-700 text-[#fdf0d5] border border-amber-700 px-4 py-2 rounded-lg text-sm hover:bg-amber-800">
        <i class="fas fa-arrow-left mr-1"></i> Back
      </a>
    </div>

    <% if (statusError) { %>
      <div class="mb-4 text-sm bg-red-900/40 border border-red-600/40 text-red-200 rounded-lg px-4 py-3"><%= statusError %></div>
    <% } %>
    <% if (statusSuccess) { %>
      <div class="mb-4 text-sm bg-emerald-900/40 border border-emerald-600/40 text-emerald-200 rounded-lg px-4 py-3"><%= statusSuccess %></div>
    <% } %>

    <div class="bg-[#fff8ea] border border-amber-300 rounded-2xl p-6 space-y-5 shadow-sm">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">Request ID</p>
          <p class="font-semibold text-amber-950"><%= requestData.id %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">Amount</p>
          <p class="font-semibold text-amber-950">Rs <%= Number(requestData.amount).toLocaleString() %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">User Name</p>
          <p class="font-semibold text-amber-950"><%= requestData.user_name %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">User Phone</p>
          <p class="font-semibold text-amber-950"><%= requestData.user_phone %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4 md:col-span-2">
          <p class="text-amber-700 mb-1">User Email</p>
          <p class="font-semibold text-amber-950 break-all"><%= requestData.user_email %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">Reference</p>
          <p class="font-semibold text-amber-950"><%= requestData.reference %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">Bank Name</p>
          <p class="font-semibold text-amber-950"><%= requestData.bank_name %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">Account Holder</p>
          <p class="font-semibold text-amber-950"><%= requestData.account_holder %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">Account Number</p>
          <p class="font-semibold text-amber-950 break-all"><%= requestData.account_number %></p>
        </div>
        <div class="bg-[#fdf0d5] border border-amber-300 rounded-xl p-4">
          <p class="text-amber-700 mb-1">IFSC Code</p>
          <p class="font-semibold text-amber-950"><%= requestData.ifsc_code %></p>
        </div>
      </div>

      <div class="pt-2 border-t border-amber-300">
        <p class="text-amber-700 text-sm mb-2">Current Status: <span class="font-semibold text-amber-950"><%= requestData.status %></span></p>
        <p class="text-amber-700 text-xs mb-4">Created: <%= new Date(requestData.created_at).toLocaleString() %></p>

        <form action="/admin/withdraw-requests/<%= requestData.id %>/status" method="POST" class="flex flex-col sm:flex-row sm:items-center gap-3">
          <select name="status" class="w-full sm:w-auto bg-[#fdf0d5] border border-amber-300 rounded-lg px-3 py-2 text-sm text-amber-950 outline-none">
            <option value="pending" <%= String(requestData.status).toLowerCase() === "pending" ? "selected" : "" %>>Pending</option>
            <option value="completed" <%= String(requestData.status).toLowerCase() === "completed" ? "selected" : "" %>>Completed</option>
            <option value="failed" <%= String(requestData.status).toLowerCase() === "failed" ? "selected" : "" %>>Failed</option>
          </select>
          <button type="submit" class="w-full sm:w-auto bg-amber-700 border border-amber-700 hover:bg-amber-800 text-[#fdf0d5] text-sm px-4 py-2 rounded-lg">
            Update Status
          </button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>


